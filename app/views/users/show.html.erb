<div id="user_info">
  <h3>User : <%= current_user.name if current_user %> </h3>   
</div>

<div>
  <%= link_to "New Picture", new_picture_path %>
  <%= link_to "New Contact", new_contact_path %>
<div>
  <br/>
<div>
  <%= link_to "View Pictures", pictures_path %>
  <%= link_to "View Contacts", contacts_path %>
<div>

</br>
</br>
<div>
    <div id="groups_list">
      <%= button_to "Create Album", new_album_path, :method => :get %>

      <ul id="user_albums" style="list-style-type: none" >
        <% current_user.albums.each do |a| %>
          <li class="interface_box" style="background-color: #FFFFFF; width:200px; height:35px"><%= link_to a.name, a %></li>
          <br/>
        <% end %>
      </ul>
    </div> 
    
    <div id="albums_list" >
      <%= button_to "Create Group", new_group_path, :method => :get %>
      <ul id="user_albums"groups style="list-style-type: none" >
        <% current_user.groups.each do |g| %>
          <li class="interface_box" style="background-color: #FFFFFF; width:200px; height:35px"><%= link_to g.name, g %></li>
          <br/>
        <% end %>
      </ul>
    </div>
</div>

<div style="color:#fff">_</div>
